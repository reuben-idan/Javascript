let height = Number(window.prompt("Enter the height of the box: "));
let width = Number(window.prompt("Enter the width of the box: "));
let depth = Number(window.prompt("Enter the depth of the box: "));
let volume = height * width * depth;
window.alert("The volume of the box is " + volume);

